{
  "name": "web-framework",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && swc src -d dist/src && swc lib -d dist/lib",
    "type-check": "tsc --noEmit",
    "start": "node dist/src/server.js",
    "dev": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.63",
    "@swc/core": "^1.3.96",
    "@types/body-parser": "^1.19.5",
    "@types/node": "^20.9.1",
    "nodemon": "^3.0.1",
    "typescript": "^5.2.2"
  }
}
